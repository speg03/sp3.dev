language: python
python:
  - 3.6
script:
  - mkdocs build --verbose --clean --strict
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  keep-history: true
  local_dir: site
  target_branch: master
  on:
    branch: source
